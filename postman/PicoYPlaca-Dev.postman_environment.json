{
  "name": "PicoYPlaca - Backend directo (puerto 8080)",
  "values": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8080",
      "type": "default",
      "enabled": true,
      "description": "Apunta directamente al backend en el puerto 8080. Funciona en dos casos: (1) backend corriendo con 'mvn spring-boot:run', o (2) Docker Compose levantado (el puerto 8080 se mapea al host). Es el entorno recomendado para probar la API."
    }
  ],
  "_postman_variable_scope": "environment"
}
